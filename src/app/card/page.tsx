"use client";

import { ProfileCard } from "@/components/profile-card";

export default function CardPage() {
  return (
    <main className="min-h-screen flex justify-center p-4 mt-16 bg-black">
      <ProfileCard />
    </main>
  );
}
